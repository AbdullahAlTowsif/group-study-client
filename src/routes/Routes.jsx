import { createBrowserRouter } from "react-router-dom";

const router = createBrowserRouter([
    {
        path: "/",
        element: <div className="text-4xl font-bold">Group Study!</div>,
    },
]);

export default router;
